#!/usr/bin/env jaiph
# E2E fixture: run pass then run fail; we only run the fail path in e2e.
def step_ok() {
  mock_ok
}
def step_fail() {
  mock_fail
}
export def main() {
  run step_ok()
  run step_fail()
}
