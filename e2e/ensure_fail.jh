#!/usr/bin/env jaiph
# E2E fixture: ensure pass then ensure fail; we only run the fail path in e2e.
workflow step_ok() {
  mock_ok
}
workflow step_fail() {
  mock_fail
}
workflow default() {
  run step_ok()
  run step_fail()
}
