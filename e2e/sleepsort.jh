#! /usr/bin/env jaiph

script sleep = "sleep $1 && echo \"$1\""

workflow delayed_log(seconds) {
  run sleep(${seconds})
  log "${seconds}"
}

workflow default {
  run async delayed_log(3)
  run async delayed_log(1)
  run async delayed_log(2)
}
