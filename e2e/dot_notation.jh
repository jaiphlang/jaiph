#!/usr/bin/env jaiph
# Fixture: dot notation for typed prompt field access.
workflow classify() {
  const result = prompt "Classify this" returns "{ category: string, confidence: number }"
  log "category=${result.category}"
  log "confidence=${result.confidence}"
  return "${result.category}"
}
