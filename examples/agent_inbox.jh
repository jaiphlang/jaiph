#!/usr/bin/env jaiph

channel findings
channel report

workflow scanner() {
  log "Scanning for issues..."
  findings <- "Found 3 issues in auth module"
}

workflow analyst() {
  log "Analyzing message from ${arg3} on channel ${arg2}..."
  report <- "Summary: ${arg1}"
}

workflow reviewer() {
  log "Reviewing message from ${arg3} on channel ${arg2}..."
  logerr "Critical issue: ${arg1}"
}

workflow default() {
  findings -> analyst
  report -> reviewer

  run scanner()
}
