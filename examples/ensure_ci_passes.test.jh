#!/usr/bin/env jaiph

import "ensure_ci_passes.jh" as ci

test "ci passes on first attempt skips recover" {
  # Given
  mock script ci.npm_run_test_ci {
    echo "all tests passed"
  }

  # When / Then — workflow completes without prompt
  ci.default
}
