title: Jaiph — language for AI-assisted development
description: >-
  Jaiph — Open source language for AI-assisted development.

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: none

highlighter: none

plugins:
  - jekyll-relative-links
  - jekyll-redirect-from

relative_links:
  enabled: true
  collections: false

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "docs"

exclude:
  - vendor
  # Keep install-from-local.sh out of the site (developer-only).
  - install-from-local.sh
  - CNAME
