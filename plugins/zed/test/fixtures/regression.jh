# Regression fixture: stale surface the grammar must NOT highlight as keywords.

export def main() {
  # Removed keywords must highlight as plain identifiers, not @keyword.
  wait for_something
  local
  rule
  workflow
  ensure
  inbox
  # The reverse arrow below is invalid (the send arrow is right-pointing only);
  # it must not tokenize as an operator.
  send "legacy" <- alerts
}
