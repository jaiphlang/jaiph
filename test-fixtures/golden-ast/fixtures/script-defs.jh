script greet = `echo hello`

script multiline = ```
echo "line 1"
echo "line 2"
```

script with_shell_expansion = ```
echo "Hello ${USER}"
echo "${PATH:-/usr/bin}"
```

workflow default() {
  run greet()
  run multiline()
}
